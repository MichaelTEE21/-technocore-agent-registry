#!/usr/bin/env python3
"""Entry point: python scripts/technocore-agent <cmd> ..."""
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from tar_cli.__main__ import main

if __name__ == "__main__":
    sys.exit(main())
