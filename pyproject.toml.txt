[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "technocore-agent-registry"
version = "1.0.0"
description = "An open-source reference implementation and proposal for agent capability discovery within the Technocore ecosystem."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "MichaelTEE21" }]
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.141.1",
  "sqlalchemy==2.0.52",
  "pydantic==2.11.7",
  "uvicorn==0.35.0",
  "httpx==0.28.1",
  "jinja2==3.1.6",
  "python-multipart==0.0.20",
  "cryptography==44.0.3",
]"psycopg2-binary==2.9.12",

[project.optional-dependencies]
dev = ["pytest==8.4.1", "ruff==0.12.12"]

[project.scripts]
technocore-agent = "tar_cli.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
tar = ["templates/*.html", "static/*", "data/*.json"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.vercel]
entrypoint = "src.tar.main:app"
